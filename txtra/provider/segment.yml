id: segment

info:
  name: segment
  author: NetSPI(https://raw.githubusercontent.com/NetSPI/PowerShell/master/Resolve-DnsDomainValidationToken.ps1)
  category: Analytics

rule:
  type: regex
  regex:
    - "segment-site-verification=(?P<token>[A-Za-z0-9=]+)"
  params:
    - token
