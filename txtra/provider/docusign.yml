id: docusign

info:
  name: docusign
  author: wild0ni0n
  category: Cloud
  references:
    - https://support.docusign.com/en/guides/org-admin-guide-domains

rule:
  type: regex
  regex:
    - "docusign=(?P<token>[A-Za-z0-9.\-]+)"
  params:
    - token
