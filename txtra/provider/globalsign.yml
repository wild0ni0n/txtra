id: globalsign

info:
  name: Global Sign
  author: wild0ni0n
  category: Certificate

rule:
  type: regex
  regex:
    - "_globalsign-domain-verification=(?P<token>[A-Za-z0-9.]+)"
  params:
    - token
