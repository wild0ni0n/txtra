id: protonmail

info:
  name: protonmail
  author: wild0ni0n
  category: Mail
  references:
    - https://protonmail.com/support/knowledge-base/dns-records/

rule:
  type: regex
  regex:
    - "protonmail-verification=(?P<token>[A-Za-z0-9\\-]+)"
  params:
    - token
