id: mailjet

info:
  name: Mailjet
  author: NetSPI(https://raw.githubusercontent.com/NetSPI/PowerShell/master/Resolve-DnsDomainValidationToken.ps1)
  category: Analytics

rule:
  type: regex
  regex:
    - "mailjet-domain-validation=(?P<token>[A-Za-z0-9]+)"
  params:
    - token
