id: apple-business-manager

info:
  name: Apple Business Manager
  author: wild0ni0n
  category: Cloud

rule:
  type: regex
  regex:
    - "apple-domain-verification=(?P<token>[A-Za-z0-9.]+)"
  params:
    - token
